name: Build, Push and Deploy Docker image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2



      - name: Debug Secrets
        run: |
          echo "DOCKER_USERNAME: ${{ deploysecrets.DOCKER_USERNAME }}"
          echo
